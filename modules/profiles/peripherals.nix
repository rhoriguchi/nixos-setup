{
  hardware = {
    logitech.wireless = {
      enable = true;
      enableGraphical = true;
    };

    # TODO currently does not build
    # openrazer.enable = true;
  };

  programs.bazecor.enable = true;
}
